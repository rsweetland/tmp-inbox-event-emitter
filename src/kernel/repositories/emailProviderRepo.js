/**
 * Class used to model email providers from the database.
 *
 * @author Dragos Sebestin
 */
class EmailProviderRepo {

  /**
   * Class constructor.
   */
  constructor (dbConnection) {}

  /**
   * Create a new email provider.
   * @param
   * @returns {Promise<void>}
   */
  create () {}
}
